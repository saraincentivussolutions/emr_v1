<?php
	echo 'Set module in the URL';
	exit;
	/*include 'app/common/class.common.php'; 
	$common = new common();
	$action = $_POST['action'];
	$module = $_POST['module'];
	
	if($module)
	{
		//ini_set('display_errors', 1);
		$template = $common->getModuleProcess($module); 
		if($template)
		include $template;
		else 
		exit('error in process page or check process control page exist');
	}*/

?>